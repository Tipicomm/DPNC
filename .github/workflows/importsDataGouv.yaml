name: 📥 Importation des données - CartoDataMC

on:
  workflow_dispatch:

jobs:
  import:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Installer Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Installer les dépendances
      run: pip install pandas requests

    - name: Exécuter le script d'import
      run: python CartoDataMC/importsDataGouv.py

    - name: ✅ Commit et push des fichiers générés
      run: |
        git config --global user.name 'github-actions'
        git config --global user.email 'actions@github.com'
        git add CartoDataMC/*.csv
        git commit -m "📊 [auto] Données importées depuis data.gouv.fr"
        git push
